// SPDX-License-Identifier: CC0-1.0

//! Auto-generated `into_model` conversions for Bitcoin Core `30` - blockchain.
//!
//! Generated by `codegen`. Do not edit by hand, re-run `just codegen` to regenerate. Each
//! conversion turns a raw response type (from the sibling `mod.rs`) into its shared,
//! version-nonspecific `crate::model` counterpart. Conversions the canonical types get
//! wrong are routed through `crate::v30::generated::compatibility`; see that file and
//! `corepc_bugs_backlog.md`.

// `unreachable_code` is allowed for the half-done `todo!()` reconstructions (see the
// per-site TODOs); they diverge, making later struct fields unreachable.
#![allow(unreachable_code, unused_imports)]

use core::fmt;

use bitcoin::address::{self, NetworkUnchecked};
use bitcoin::consensus::encode;
use bitcoin::error::UnprefixedHexError;
use bitcoin::hashes::{hash160, sha256};
use bitcoin::{
    amount, block, hex, network, psbt, Address, Amount, Block, BlockHash, CompactTarget, FeeRate,
    Network, Psbt, ScriptBuf, Sequence, SignedAmount, Target, Transaction, TxMerkleNode, Txid,
    Weight, Work, Wtxid,
};

use super::*;
use crate::error::write_err;
use crate::model;
use crate::v30::generated::compatibility;

impl DumpTxOutSet {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::DumpTxOutSet, DumpTxOutSetError> {
        use DumpTxOutSetError as E;

        Ok(model::DumpTxOutSet {
            // codegen-correct conversion (u64): `self.coins_written`.
            // TODO(compat): canonical types `coins_written` as `Amount` but Core returns a UTXO count (corepc_bugs_backlog.md #1). Routed through a wrong placeholder in `compatibility` to compile.
            coins_written: compatibility::dump_tx_out_set_coins_written(self.coins_written),
            base_hash: self.base_hash.parse::<BlockHash>().map_err(E::BaseHash)?,
            base_height: crate::to_u32(self.base_height, "base_height")?,
            path: self.path,
            tx_out_set_hash: self
                .tx_out_set_hash
                .parse::<sha256::Hash>()
                .map_err(E::TxOutSetHash)?,
            n_chain_tx: u32::try_from(self.n_chain_tx).map_err(|_| {
                crate::NumericError::Overflow {
                    value: self.n_chain_tx as i64,
                    field: "n_chain_tx".to_owned(),
                }
            })?,
        })
    }
}

/// Error when converting a `DumpTxOutSet` type into the model type.
#[derive(Debug)]
pub enum DumpTxOutSetError {
    /// Conversion of the `BaseHash` field failed.
    BaseHash(hex::HexToArrayError),
    /// Conversion of the `TxOutSetHash` field failed.
    TxOutSetHash(hex::HexToArrayError),
    /// Conversion of a numeric type to the expected type failed.
    Numeric(crate::NumericError),
}

impl fmt::Display for DumpTxOutSetError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::BaseHash(ref e) => write_err!(f, "conversion of the `BaseHash` field failed"; e),
            Self::TxOutSetHash(ref e) =>
                write_err!(f, "conversion of the `TxOutSetHash` field failed"; e),
            Self::Numeric(ref e) => write_err!(f, "numeric conversion failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for DumpTxOutSetError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::BaseHash(ref e) => Some(e),
            Self::TxOutSetHash(ref e) => Some(e),
            Self::Numeric(ref e) => Some(e),
        }
    }
}

impl From<crate::NumericError> for DumpTxOutSetError {
    fn from(e: crate::NumericError) -> Self { Self::Numeric(e) }
}

impl GetBestBlockHash {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::GetBestBlockHash, GetBestBlockHashError> {
        use GetBestBlockHashError as E;

        Ok(model::GetBestBlockHash(self.0.parse::<BlockHash>().map_err(E::Inner)?))
    }
}

/// Error when converting a `GetBestBlockHash` type into the model type.
#[derive(Debug)]
pub enum GetBestBlockHashError {
    /// Conversion of the `Inner` field failed.
    Inner(hex::HexToArrayError),
}

impl fmt::Display for GetBestBlockHashError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Inner(ref e) => write_err!(f, "conversion of the `Inner` field failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for GetBestBlockHashError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Inner(ref e) => Some(e),
        }
    }
}

impl GetBlockCount {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::GetBlockCount, GetBlockCountError> {
        use GetBlockCountError as E;

        Ok(model::GetBlockCount(crate::to_u64(self.0, "inner")?))
    }
}

/// Error when converting a `GetBlockCount` type into the model type.
#[derive(Debug)]
pub enum GetBlockCountError {
    /// Conversion of a numeric type to the expected type failed.
    Numeric(crate::NumericError),
}

impl fmt::Display for GetBlockCountError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Numeric(ref e) => write_err!(f, "numeric conversion failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for GetBlockCountError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Numeric(ref e) => Some(e),
        }
    }
}

impl From<crate::NumericError> for GetBlockCountError {
    fn from(e: crate::NumericError) -> Self { Self::Numeric(e) }
}

impl GetBlockHash {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::GetBlockHash, GetBlockHashError> {
        use GetBlockHashError as E;

        Ok(model::GetBlockHash(self.0.parse::<BlockHash>().map_err(E::Inner)?))
    }
}

/// Error when converting a `GetBlockHash` type into the model type.
#[derive(Debug)]
pub enum GetBlockHashError {
    /// Conversion of the `Inner` field failed.
    Inner(hex::HexToArrayError),
}

impl fmt::Display for GetBlockHashError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Inner(ref e) => write_err!(f, "conversion of the `Inner` field failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for GetBlockHashError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Inner(ref e) => Some(e),
        }
    }
}

impl GetBlockHeaderVerbose0 {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::GetBlockHeader, GetBlockHeaderError> {
        use GetBlockHeaderError as E;

        Ok(model::GetBlockHeader(
            encode::deserialize_hex::<block::Header>(&self.0).map_err(E::Inner)?,
        ))
    }
}

/// Error when converting a `GetBlockHeader` type into the model type.
#[derive(Debug)]
pub enum GetBlockHeaderError {
    /// Conversion of the `Inner` field failed.
    Inner(encode::FromHexError),
}

impl fmt::Display for GetBlockHeaderError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Inner(ref e) => write_err!(f, "conversion of the `Inner` field failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for GetBlockHeaderError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Inner(ref e) => Some(e),
        }
    }
}

impl GetBlockHeaderVerbose1 {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::GetBlockHeaderVerbose, GetBlockHeaderVerboseError> {
        use GetBlockHeaderVerboseError as E;

        Ok(model::GetBlockHeaderVerbose {
            hash: self.hash.parse::<BlockHash>().map_err(E::Hash)?,
            confirmations: self.confirmations,
            height: crate::to_u32(self.height, "height")?,
            version: block::Version::from_consensus(self.version as i32),
            merkle_root: self.merkle_root.parse::<TxMerkleNode>().map_err(E::MerkleRoot)?,
            time: crate::to_u32(self.time, "time")?,
            median_time: crate::to_u32(self.median_time, "median_time")?,
            nonce: crate::to_u32(self.nonce, "nonce")?,
            bits: CompactTarget::from_unprefixed_hex(&self.bits).map_err(E::Bits)?,
            target: Some(Target::from_unprefixed_hex(&self.target).map_err(E::Target)?),
            difficulty: self.difficulty,
            chain_work: Work::from_unprefixed_hex(&self.chain_work).map_err(E::ChainWork)?,
            n_tx: crate::to_u32(self.n_tx, "n_tx")?,
            previous_block_hash: self
                .previous_block_hash
                .map(|x| x.parse::<BlockHash>())
                .transpose()
                .map_err(E::PreviousBlockHash)?,
            next_block_hash: self
                .next_block_hash
                .map(|x| x.parse::<BlockHash>())
                .transpose()
                .map_err(E::NextBlockHash)?,
        })
    }
}

/// Error when converting a `GetBlockHeaderVerbose` type into the model type.
#[derive(Debug)]
pub enum GetBlockHeaderVerboseError {
    /// Conversion of the `Hash` field failed.
    Hash(hex::HexToArrayError),
    /// Conversion of the `MerkleRoot` field failed.
    MerkleRoot(hex::HexToArrayError),
    /// Conversion of the `Bits` field failed.
    Bits(UnprefixedHexError),
    /// Conversion of the `Target` field failed.
    Target(UnprefixedHexError),
    /// Conversion of the `ChainWork` field failed.
    ChainWork(UnprefixedHexError),
    /// Conversion of the `PreviousBlockHash` field failed.
    PreviousBlockHash(hex::HexToArrayError),
    /// Conversion of the `NextBlockHash` field failed.
    NextBlockHash(hex::HexToArrayError),
    /// Conversion of a numeric type to the expected type failed.
    Numeric(crate::NumericError),
}

impl fmt::Display for GetBlockHeaderVerboseError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Hash(ref e) => write_err!(f, "conversion of the `Hash` field failed"; e),
            Self::MerkleRoot(ref e) =>
                write_err!(f, "conversion of the `MerkleRoot` field failed"; e),
            Self::Bits(ref e) => write_err!(f, "conversion of the `Bits` field failed"; e),
            Self::Target(ref e) => write_err!(f, "conversion of the `Target` field failed"; e),
            Self::ChainWork(ref e) =>
                write_err!(f, "conversion of the `ChainWork` field failed"; e),
            Self::PreviousBlockHash(ref e) =>
                write_err!(f, "conversion of the `PreviousBlockHash` field failed"; e),
            Self::NextBlockHash(ref e) =>
                write_err!(f, "conversion of the `NextBlockHash` field failed"; e),
            Self::Numeric(ref e) => write_err!(f, "numeric conversion failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for GetBlockHeaderVerboseError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Hash(ref e) => Some(e),
            Self::MerkleRoot(ref e) => Some(e),
            Self::Bits(ref e) => Some(e),
            Self::Target(ref e) => Some(e),
            Self::ChainWork(ref e) => Some(e),
            Self::PreviousBlockHash(ref e) => Some(e),
            Self::NextBlockHash(ref e) => Some(e),
            Self::Numeric(ref e) => Some(e),
        }
    }
}

impl From<crate::NumericError> for GetBlockHeaderVerboseError {
    fn from(e: crate::NumericError) -> Self { Self::Numeric(e) }
}

impl GetBlockVerbose0 {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::GetBlockVerboseZero, GetBlockVerboseZeroError> {
        use GetBlockVerboseZeroError as E;

        Ok(model::GetBlockVerboseZero(encode::deserialize_hex::<Block>(&self.0).map_err(E::Inner)?))
    }
}

/// Error when converting a `GetBlockVerboseZero` type into the model type.
#[derive(Debug)]
pub enum GetBlockVerboseZeroError {
    /// Conversion of the `Inner` field failed.
    Inner(encode::FromHexError),
}

impl fmt::Display for GetBlockVerboseZeroError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Inner(ref e) => write_err!(f, "conversion of the `Inner` field failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for GetBlockVerboseZeroError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Inner(ref e) => Some(e),
        }
    }
}

impl GetBlockVerbose1 {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::GetBlockVerboseOne, GetBlockVerboseOneError> {
        use GetBlockVerboseOneError as E;

        Ok(model::GetBlockVerboseOne {
            hash: self.hash.parse::<BlockHash>().map_err(E::Hash)?,
            confirmations: self.confirmations,
            size: u32::try_from(self.size).map_err(|_| crate::NumericError::Overflow {
                value: self.size as i64,
                field: "size".to_owned(),
            })?,
            stripped_size: Some(u32::try_from(self.stripped_size).map_err(|_| {
                crate::NumericError::Overflow {
                    value: self.stripped_size as i64,
                    field: "stripped_size".to_owned(),
                }
            })?),
            weight: Weight::from_wu(self.weight as u64),
            height: crate::to_u32(self.height, "height")?,
            version: block::Version::from_consensus(self.version as i32),
            merkle_root: self.merkle_root.parse::<TxMerkleNode>().map_err(E::MerkleRoot)?,
            tx: self
                .tx
                .into_iter()
                .map(|x| x.parse::<Txid>().map_err(E::Tx))
                .collect::<Result<Vec<_>, _>>()?,
            time: crate::to_u32(self.time, "time")?,
            median_time: Some(crate::to_u32(self.median_time, "median_time")?),
            nonce: crate::to_u32(self.nonce, "nonce")?,
            bits: CompactTarget::from_unprefixed_hex(&self.bits).map_err(E::Bits)?,
            target: Some(Target::from_unprefixed_hex(&self.target).map_err(E::Target)?),
            difficulty: self.difficulty,
            chain_work: Work::from_unprefixed_hex(&self.chain_work).map_err(E::ChainWork)?,
            n_tx: crate::to_u32(self.n_tx, "n_tx")?,
            previous_block_hash: self
                .previous_block_hash
                .map(|x| x.parse::<BlockHash>())
                .transpose()
                .map_err(E::PreviousBlockHash)?,
            next_block_hash: self
                .next_block_hash
                .map(|x| x.parse::<BlockHash>())
                .transpose()
                .map_err(E::NextBlockHash)?,
        })
    }
}

/// Error when converting a `GetBlockVerboseOne` type into the model type.
#[derive(Debug)]
pub enum GetBlockVerboseOneError {
    /// Conversion of the `Hash` field failed.
    Hash(hex::HexToArrayError),
    /// Conversion of the `MerkleRoot` field failed.
    MerkleRoot(hex::HexToArrayError),
    /// Conversion of the `Tx` field failed.
    Tx(hex::HexToArrayError),
    /// Conversion of the `Bits` field failed.
    Bits(UnprefixedHexError),
    /// Conversion of the `Target` field failed.
    Target(UnprefixedHexError),
    /// Conversion of the `ChainWork` field failed.
    ChainWork(UnprefixedHexError),
    /// Conversion of the `PreviousBlockHash` field failed.
    PreviousBlockHash(hex::HexToArrayError),
    /// Conversion of the `NextBlockHash` field failed.
    NextBlockHash(hex::HexToArrayError),
    /// Conversion of a numeric type to the expected type failed.
    Numeric(crate::NumericError),
}

impl fmt::Display for GetBlockVerboseOneError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Hash(ref e) => write_err!(f, "conversion of the `Hash` field failed"; e),
            Self::MerkleRoot(ref e) =>
                write_err!(f, "conversion of the `MerkleRoot` field failed"; e),
            Self::Tx(ref e) => write_err!(f, "conversion of the `Tx` field failed"; e),
            Self::Bits(ref e) => write_err!(f, "conversion of the `Bits` field failed"; e),
            Self::Target(ref e) => write_err!(f, "conversion of the `Target` field failed"; e),
            Self::ChainWork(ref e) =>
                write_err!(f, "conversion of the `ChainWork` field failed"; e),
            Self::PreviousBlockHash(ref e) =>
                write_err!(f, "conversion of the `PreviousBlockHash` field failed"; e),
            Self::NextBlockHash(ref e) =>
                write_err!(f, "conversion of the `NextBlockHash` field failed"; e),
            Self::Numeric(ref e) => write_err!(f, "numeric conversion failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for GetBlockVerboseOneError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Hash(ref e) => Some(e),
            Self::MerkleRoot(ref e) => Some(e),
            Self::Tx(ref e) => Some(e),
            Self::Bits(ref e) => Some(e),
            Self::Target(ref e) => Some(e),
            Self::ChainWork(ref e) => Some(e),
            Self::PreviousBlockHash(ref e) => Some(e),
            Self::NextBlockHash(ref e) => Some(e),
            Self::Numeric(ref e) => Some(e),
        }
    }
}

impl From<crate::NumericError> for GetBlockVerboseOneError {
    fn from(e: crate::NumericError) -> Self { Self::Numeric(e) }
}

impl GetBlockchainInfo {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::GetBlockchainInfo, GetBlockchainInfoError> {
        use GetBlockchainInfoError as E;

        Ok(model::GetBlockchainInfo {
            chain: Network::from_core_arg(&self.chain).map_err(E::Chain)?,
            blocks: crate::to_u32(self.blocks, "blocks")?,
            headers: crate::to_u32(self.headers, "headers")?,
            best_block_hash: self.best_block_hash.parse::<BlockHash>().map_err(E::BestBlockHash)?,
            bits: Some(CompactTarget::from_unprefixed_hex(&self.bits).map_err(E::Bits)?),
            target: Some(Target::from_unprefixed_hex(&self.target).map_err(E::Target)?),
            difficulty: self.difficulty,
            time: Some(crate::to_u32(self.time, "time")?),
            median_time: crate::to_u32(self.median_time, "median_time")?,
            verification_progress: self.verification_progress,
            initial_block_download: self.initial_block_download,
            chain_work: Work::from_unprefixed_hex(&self.chain_work).map_err(E::ChainWork)?,
            size_on_disk: self.size_on_disk,
            pruned: self.pruned,
            prune_height: self
                .prune_height
                .map(|x| crate::to_u32(x, "prune_height"))
                .transpose()?,
            automatic_pruning: self.automatic_pruning,
            prune_target_size: self.prune_target_size,
            softforks: Default::default(), // no raw field; canonical is a map
            signet_challenge: self
                .signet_challenge
                .map(|x| ScriptBuf::from_hex(&x))
                .transpose()
                .map_err(E::SignetChallenge)?,
            warnings: self.warnings,
        })
    }
}

/// Error when converting a `GetBlockchainInfo` type into the model type.
#[derive(Debug)]
pub enum GetBlockchainInfoError {
    /// Conversion of the `Chain` field failed.
    Chain(network::ParseNetworkError),
    /// Conversion of the `BestBlockHash` field failed.
    BestBlockHash(hex::HexToArrayError),
    /// Conversion of the `Bits` field failed.
    Bits(UnprefixedHexError),
    /// Conversion of the `Target` field failed.
    Target(UnprefixedHexError),
    /// Conversion of the `ChainWork` field failed.
    ChainWork(UnprefixedHexError),
    /// Conversion of the `SignetChallenge` field failed.
    SignetChallenge(hex::HexToBytesError),
    /// Conversion of a numeric type to the expected type failed.
    Numeric(crate::NumericError),
}

impl fmt::Display for GetBlockchainInfoError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Chain(ref e) => write_err!(f, "conversion of the `Chain` field failed"; e),
            Self::BestBlockHash(ref e) =>
                write_err!(f, "conversion of the `BestBlockHash` field failed"; e),
            Self::Bits(ref e) => write_err!(f, "conversion of the `Bits` field failed"; e),
            Self::Target(ref e) => write_err!(f, "conversion of the `Target` field failed"; e),
            Self::ChainWork(ref e) =>
                write_err!(f, "conversion of the `ChainWork` field failed"; e),
            Self::SignetChallenge(ref e) =>
                write_err!(f, "conversion of the `SignetChallenge` field failed"; e),
            Self::Numeric(ref e) => write_err!(f, "numeric conversion failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for GetBlockchainInfoError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Chain(ref e) => Some(e),
            Self::BestBlockHash(ref e) => Some(e),
            Self::Bits(ref e) => Some(e),
            Self::Target(ref e) => Some(e),
            Self::ChainWork(ref e) => Some(e),
            Self::SignetChallenge(ref e) => Some(e),
            Self::Numeric(ref e) => Some(e),
        }
    }
}

impl From<crate::NumericError> for GetBlockchainInfoError {
    fn from(e: crate::NumericError) -> Self { Self::Numeric(e) }
}

impl GetChainStates {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::GetChainStates, GetChainStatesError> {
        use GetChainStatesError as E;

        Ok(model::GetChainStates {
            headers: crate::to_u32(self.headers, "headers")?,
            chain_states: self
                .chain_states
                .into_iter()
                .map(|x| x.into_model().map_err(E::ChainStates))
                .collect::<Result<Vec<_>, _>>()?,
        })
    }
}

/// Error when converting a `GetChainStates` type into the model type.
#[derive(Debug)]
pub enum GetChainStatesError {
    /// Conversion of the `ChainStates` field failed.
    ChainStates(ChainStateError),
    /// Conversion of a numeric type to the expected type failed.
    Numeric(crate::NumericError),
}

impl fmt::Display for GetChainStatesError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::ChainStates(ref e) =>
                write_err!(f, "conversion of the `ChainStates` field failed"; e),
            Self::Numeric(ref e) => write_err!(f, "numeric conversion failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for GetChainStatesError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::ChainStates(ref e) => Some(e),
            Self::Numeric(ref e) => Some(e),
        }
    }
}

impl From<crate::NumericError> for GetChainStatesError {
    fn from(e: crate::NumericError) -> Self { Self::Numeric(e) }
}

impl GetChainStatesChainStatesItem {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::ChainState, ChainStateError> {
        use ChainStateError as E;

        Ok(model::ChainState {
            blocks: crate::to_u32(self.blocks, "blocks")?,
            best_block_hash: self.best_block_hash.parse::<BlockHash>().map_err(E::BestBlockHash)?,
            bits: Some(CompactTarget::from_unprefixed_hex(&self.bits).map_err(E::Bits)?),
            target: Some(Target::from_unprefixed_hex(&self.target).map_err(E::Target)?),
            difficulty: self.difficulty,
            verification_progress: self.verification_progress,
            snapshot_block_hash: self
                .snapshot_block_hash
                .map(|x| x.parse::<BlockHash>())
                .transpose()
                .map_err(E::SnapshotBlockHash)?,
            coins_db_cache_bytes: self.coins_db_cache_bytes,
            coins_tip_cache_bytes: self.coins_tip_cache_bytes,
            validated: self.validated,
        })
    }
}

/// Error when converting a `ChainState` type into the model type.
#[derive(Debug)]
pub enum ChainStateError {
    /// Conversion of the `BestBlockHash` field failed.
    BestBlockHash(hex::HexToArrayError),
    /// Conversion of the `Bits` field failed.
    Bits(UnprefixedHexError),
    /// Conversion of the `Target` field failed.
    Target(UnprefixedHexError),
    /// Conversion of the `SnapshotBlockHash` field failed.
    SnapshotBlockHash(hex::HexToArrayError),
    /// Conversion of a numeric type to the expected type failed.
    Numeric(crate::NumericError),
}

impl fmt::Display for ChainStateError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::BestBlockHash(ref e) =>
                write_err!(f, "conversion of the `BestBlockHash` field failed"; e),
            Self::Bits(ref e) => write_err!(f, "conversion of the `Bits` field failed"; e),
            Self::Target(ref e) => write_err!(f, "conversion of the `Target` field failed"; e),
            Self::SnapshotBlockHash(ref e) =>
                write_err!(f, "conversion of the `SnapshotBlockHash` field failed"; e),
            Self::Numeric(ref e) => write_err!(f, "numeric conversion failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for ChainStateError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::BestBlockHash(ref e) => Some(e),
            Self::Bits(ref e) => Some(e),
            Self::Target(ref e) => Some(e),
            Self::SnapshotBlockHash(ref e) => Some(e),
            Self::Numeric(ref e) => Some(e),
        }
    }
}

impl From<crate::NumericError> for ChainStateError {
    fn from(e: crate::NumericError) -> Self { Self::Numeric(e) }
}

impl GetChainTxStats {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::GetChainTxStats, GetChainTxStatsError> {
        use GetChainTxStatsError as E;

        Ok(model::GetChainTxStats {
            time: crate::to_u32(self.time, "time")?,
            tx_count: self
                .tx_count
                .map(|x| {
                    u32::try_from(x).map_err(|_| crate::NumericError::Overflow {
                        value: x as i64,
                        field: "tx_count".to_owned(),
                    })
                })
                .transpose()?
                .unwrap_or_default(),
            window_final_block_hash: self
                .window_final_block_hash
                .parse::<BlockHash>()
                .map_err(E::WindowFinalBlockHash)?,
            window_final_block_height: Some(crate::to_u32(
                self.window_final_block_height,
                "window_final_block_height",
            )?),
            window_block_count: crate::to_u32(self.window_block_count, "window_block_count")?,
            window_tx_count: self
                .window_tx_count
                .map(|x| {
                    u32::try_from(x).map_err(|_| crate::NumericError::Overflow {
                        value: x as i64,
                        field: "window_tx_count".to_owned(),
                    })
                })
                .transpose()?,
            window_interval: self
                .window_interval
                .map(|x| crate::to_u32(x, "window_interval"))
                .transpose()?,
            tx_rate: self.tx_rate,
        })
    }
}

/// Error when converting a `GetChainTxStats` type into the model type.
#[derive(Debug)]
pub enum GetChainTxStatsError {
    /// Conversion of the `WindowFinalBlockHash` field failed.
    WindowFinalBlockHash(hex::HexToArrayError),
    /// Conversion of a numeric type to the expected type failed.
    Numeric(crate::NumericError),
}

impl fmt::Display for GetChainTxStatsError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::WindowFinalBlockHash(ref e) =>
                write_err!(f, "conversion of the `WindowFinalBlockHash` field failed"; e),
            Self::Numeric(ref e) => write_err!(f, "numeric conversion failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for GetChainTxStatsError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::WindowFinalBlockHash(ref e) => Some(e),
            Self::Numeric(ref e) => Some(e),
        }
    }
}

impl From<crate::NumericError> for GetChainTxStatsError {
    fn from(e: crate::NumericError) -> Self { Self::Numeric(e) }
}

impl GetDeploymentInfo {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::GetDeploymentInfo, GetDeploymentInfoError> {
        use GetDeploymentInfoError as E;

        Ok(model::GetDeploymentInfo {
            hash: self.hash.parse::<BlockHash>().map_err(E::Hash)?,
            height: crate::to_u32(self.height, "height")?,
            deployments: self
                .deployments
                .into_iter()
                .map(|(k, v)| Ok::<_, E>((k, v.into_model().map_err(E::DeploymentsValue)?)))
                .collect::<Result<std::collections::BTreeMap<_, _>, _>>()?,
        })
    }
}

/// Error when converting a `GetDeploymentInfo` type into the model type.
#[derive(Debug)]
pub enum GetDeploymentInfoError {
    /// Conversion of the `Hash` field failed.
    Hash(hex::HexToArrayError),
    /// Conversion of the `DeploymentsValue` field failed.
    DeploymentsValue(DeploymentInfoError),
    /// Conversion of a numeric type to the expected type failed.
    Numeric(crate::NumericError),
}

impl fmt::Display for GetDeploymentInfoError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Hash(ref e) => write_err!(f, "conversion of the `Hash` field failed"; e),
            Self::DeploymentsValue(ref e) =>
                write_err!(f, "conversion of the `DeploymentsValue` field failed"; e),
            Self::Numeric(ref e) => write_err!(f, "numeric conversion failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for GetDeploymentInfoError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Hash(ref e) => Some(e),
            Self::DeploymentsValue(ref e) => Some(e),
            Self::Numeric(ref e) => Some(e),
        }
    }
}

impl From<crate::NumericError> for GetDeploymentInfoError {
    fn from(e: crate::NumericError) -> Self { Self::Numeric(e) }
}

impl GetDeploymentInfoDeployments {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::DeploymentInfo, DeploymentInfoError> {
        use DeploymentInfoError as E;

        Ok(model::DeploymentInfo {
            deployment_type: self.type_,
            height: self.height.map(|x| crate::to_u32(x, "height")).transpose()?,
            active: self.active,
            bip9: self.bip9.map(|x| x.into_model()).transpose().map_err(E::Bip9)?,
        })
    }
}

/// Error when converting a `DeploymentInfo` type into the model type.
#[derive(Debug)]
pub enum DeploymentInfoError {
    /// Conversion of the `Bip9` field failed.
    Bip9(Bip9InfoError),
    /// Conversion of a numeric type to the expected type failed.
    Numeric(crate::NumericError),
}

impl fmt::Display for DeploymentInfoError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Bip9(ref e) => write_err!(f, "conversion of the `Bip9` field failed"; e),
            Self::Numeric(ref e) => write_err!(f, "numeric conversion failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for DeploymentInfoError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Bip9(ref e) => Some(e),
            Self::Numeric(ref e) => Some(e),
        }
    }
}

impl From<crate::NumericError> for DeploymentInfoError {
    fn from(e: crate::NumericError) -> Self { Self::Numeric(e) }
}

impl GetDeploymentInfoDeploymentsBip9 {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::Bip9Info, Bip9InfoError> {
        use Bip9InfoError as E;

        Ok(model::Bip9Info {
            bit: self
                .bit
                .map(|x| {
                    u8::try_from(x).map_err(|_| crate::NumericError::Overflow {
                        value: x,
                        field: "bit".to_owned(),
                    })
                })
                .transpose()?,
            start_time: self.start_time,
            timeout: self.time_out,
            min_activation_height: crate::to_u32(
                self.min_activation_height,
                "min_activation_height",
            )?,
            status: self.status,
            since: crate::to_u32(self.since, "since")?,
            status_next: self.status_next,
            statistics: self
                .statistics
                .map(|x| x.into_model())
                .transpose()
                .map_err(E::Statistics)?,
            signalling: self.signalling,
        })
    }
}

/// Error when converting a `Bip9Info` type into the model type.
#[derive(Debug)]
pub enum Bip9InfoError {
    /// Conversion of the `Statistics` field failed.
    Statistics(Bip9StatisticsError),
    /// Conversion of a numeric type to the expected type failed.
    Numeric(crate::NumericError),
}

impl fmt::Display for Bip9InfoError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Statistics(ref e) =>
                write_err!(f, "conversion of the `Statistics` field failed"; e),
            Self::Numeric(ref e) => write_err!(f, "numeric conversion failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for Bip9InfoError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Statistics(ref e) => Some(e),
            Self::Numeric(ref e) => Some(e),
        }
    }
}

impl From<crate::NumericError> for Bip9InfoError {
    fn from(e: crate::NumericError) -> Self { Self::Numeric(e) }
}

impl GetDeploymentInfoDeploymentsBip9Statistics {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::Bip9Statistics, Bip9StatisticsError> {
        use Bip9StatisticsError as E;

        Ok(model::Bip9Statistics {
            period: crate::to_u32(self.period, "period")?,
            threshold: self.threshold.map(|x| crate::to_u32(x, "threshold")).transpose()?,
            elapsed: crate::to_u32(self.elapsed, "elapsed")?,
            count: crate::to_u32(self.count, "count")?,
            possible: self.possible,
        })
    }
}

/// Error when converting a `Bip9Statistics` type into the model type.
#[derive(Debug)]
pub enum Bip9StatisticsError {
    /// Conversion of a numeric type to the expected type failed.
    Numeric(crate::NumericError),
}

impl fmt::Display for Bip9StatisticsError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Numeric(ref e) => write_err!(f, "numeric conversion failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for Bip9StatisticsError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Numeric(ref e) => Some(e),
        }
    }
}

impl From<crate::NumericError> for Bip9StatisticsError {
    fn from(e: crate::NumericError) -> Self { Self::Numeric(e) }
}

impl GetDifficulty {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::GetDifficulty, GetDifficultyError> {
        use GetDifficultyError as E;

        Ok(model::GetDifficulty(self.0))
    }
}

/// Error when converting a `GetDifficulty` type into the model type.
#[derive(Debug)]
pub enum GetDifficultyError {}

impl fmt::Display for GetDifficultyError {
    fn fmt(&self, _f: &mut fmt::Formatter) -> fmt::Result { match *self {} }
}

#[cfg(feature = "std")]
impl std::error::Error for GetDifficultyError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> { match *self {} }
}

impl GetMempoolAncestorsVerbose0 {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::GetMempoolAncestors, GetMempoolAncestorsError> {
        use GetMempoolAncestorsError as E;

        Ok(model::GetMempoolAncestors(
            self.0
                .into_iter()
                .map(|x| x.parse::<Txid>().map_err(E::Inner))
                .collect::<Result<Vec<_>, _>>()?,
        ))
    }
}

/// Error when converting a `GetMempoolAncestors` type into the model type.
#[derive(Debug)]
pub enum GetMempoolAncestorsError {
    /// Conversion of the `Inner` field failed.
    Inner(hex::HexToArrayError),
}

impl fmt::Display for GetMempoolAncestorsError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Inner(ref e) => write_err!(f, "conversion of the `Inner` field failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for GetMempoolAncestorsError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Inner(ref e) => Some(e),
        }
    }
}

impl GetMempoolAncestorsVerbose1 {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(
        self,
    ) -> Result<model::GetMempoolAncestorsVerbose, GetMempoolAncestorsVerboseError> {
        use GetMempoolAncestorsVerboseError as E;

        Ok(model::GetMempoolAncestorsVerbose(
            self.0
                .into_iter()
                .map(|(k, v)| {
                    Ok::<_, E>((
                        k.parse::<Txid>().map_err(E::InnerKey)?,
                        v.into_model().map_err(E::InnerValue)?,
                    ))
                })
                .collect::<Result<std::collections::BTreeMap<_, _>, _>>()?,
        ))
    }
}

/// Error when converting a `GetMempoolAncestorsVerbose` type into the model type.
#[derive(Debug)]
pub enum GetMempoolAncestorsVerboseError {
    /// Conversion of the `InnerKey` field failed.
    InnerKey(hex::HexToArrayError),
    /// Conversion of the `InnerValue` field failed.
    InnerValue(MempoolEntryError),
}

impl fmt::Display for GetMempoolAncestorsVerboseError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::InnerKey(ref e) => write_err!(f, "conversion of the `InnerKey` field failed"; e),
            Self::InnerValue(ref e) =>
                write_err!(f, "conversion of the `InnerValue` field failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for GetMempoolAncestorsVerboseError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::InnerKey(ref e) => Some(e),
            Self::InnerValue(ref e) => Some(e),
        }
    }
}

impl GetMempoolAncestorsVerbose1Entry {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::MempoolEntry, MempoolEntryError> {
        use MempoolEntryError as E;

        Ok(model::MempoolEntry {
            vsize: Some(crate::to_u32(self.vsize, "vsize")?),
            size: None, // no raw field; canonical is optional
            weight: Some(crate::to_u32(self.weight, "weight")?),
            time: crate::to_u32(self.time, "time")?,
            height: crate::to_u32(self.height, "height")?,
            descendant_count: u32::try_from(self.descendant_count).map_err(|_| {
                crate::NumericError::Overflow {
                    value: self.descendant_count as i64,
                    field: "descendant_count".to_owned(),
                }
            })?,
            descendant_size: u32::try_from(self.descendant_size).map_err(|_| {
                crate::NumericError::Overflow {
                    value: self.descendant_size as i64,
                    field: "descendant_size".to_owned(),
                }
            })?,
            ancestor_count: u32::try_from(self.ancestor_count).map_err(|_| {
                crate::NumericError::Overflow {
                    value: self.ancestor_count as i64,
                    field: "ancestor_count".to_owned(),
                }
            })?,
            ancestor_size: u32::try_from(self.ancestor_size).map_err(|_| {
                crate::NumericError::Overflow {
                    value: self.ancestor_size as i64,
                    field: "ancestor_size".to_owned(),
                }
            })?,
            wtxid: self.wtxid.parse::<Wtxid>().map_err(E::Wtxid)?,
            fees: self.fees.into_model().map_err(E::Fees)?,
            depends: self
                .depends
                .into_iter()
                .map(|x| x.parse::<Txid>().map_err(E::Depends))
                .collect::<Result<Vec<_>, _>>()?,
            spent_by: self
                .spent_by
                .into_iter()
                .map(|x| x.parse::<Txid>().map_err(E::SpentBy))
                .collect::<Result<Vec<_>, _>>()?,
            bip125_replaceable: Some(self.bip125_replaceable),
            unbroadcast: Some(self.unbroadcast),
        })
    }
}

/// Error when converting a `MempoolEntry` type into the model type.
#[derive(Debug)]
pub enum MempoolEntryError {
    /// Conversion of the `Wtxid` field failed.
    Wtxid(hex::HexToArrayError),
    /// Conversion of the `Fees` field failed.
    Fees(MempoolEntryFeesError),
    /// Conversion of the `Depends` field failed.
    Depends(hex::HexToArrayError),
    /// Conversion of the `SpentBy` field failed.
    SpentBy(hex::HexToArrayError),
    /// Conversion of a numeric type to the expected type failed.
    Numeric(crate::NumericError),
}

impl fmt::Display for MempoolEntryError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Wtxid(ref e) => write_err!(f, "conversion of the `Wtxid` field failed"; e),
            Self::Fees(ref e) => write_err!(f, "conversion of the `Fees` field failed"; e),
            Self::Depends(ref e) => write_err!(f, "conversion of the `Depends` field failed"; e),
            Self::SpentBy(ref e) => write_err!(f, "conversion of the `SpentBy` field failed"; e),
            Self::Numeric(ref e) => write_err!(f, "numeric conversion failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for MempoolEntryError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Wtxid(ref e) => Some(e),
            Self::Fees(ref e) => Some(e),
            Self::Depends(ref e) => Some(e),
            Self::SpentBy(ref e) => Some(e),
            Self::Numeric(ref e) => Some(e),
        }
    }
}

impl From<crate::NumericError> for MempoolEntryError {
    fn from(e: crate::NumericError) -> Self { Self::Numeric(e) }
}

impl GetMempoolAncestorsVerbose1EntryFees {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::MempoolEntryFees, MempoolEntryFeesError> {
        use MempoolEntryFeesError as E;

        Ok(model::MempoolEntryFees {
            base: Amount::from_btc(self.base).map_err(E::Base)?,
            modified: Amount::from_btc(self.modified).map_err(E::Modified)?,
            ancestor: Amount::from_btc(self.ancestor).map_err(E::Ancestor)?,
            descendant: Amount::from_btc(self.descendant).map_err(E::Descendant)?,
        })
    }
}

impl GetMempoolDescendantsVerbose0 {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::GetMempoolDescendants, GetMempoolDescendantsError> {
        use GetMempoolDescendantsError as E;

        Ok(model::GetMempoolDescendants(
            self.0
                .into_iter()
                .map(|x| x.parse::<Txid>().map_err(E::Inner))
                .collect::<Result<Vec<_>, _>>()?,
        ))
    }
}

/// Error when converting a `GetMempoolDescendants` type into the model type.
#[derive(Debug)]
pub enum GetMempoolDescendantsError {
    /// Conversion of the `Inner` field failed.
    Inner(hex::HexToArrayError),
}

impl fmt::Display for GetMempoolDescendantsError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Inner(ref e) => write_err!(f, "conversion of the `Inner` field failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for GetMempoolDescendantsError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Inner(ref e) => Some(e),
        }
    }
}

impl GetMempoolDescendantsVerbose1 {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(
        self,
    ) -> Result<model::GetMempoolDescendantsVerbose, GetMempoolDescendantsVerboseError> {
        use GetMempoolDescendantsVerboseError as E;

        Ok(model::GetMempoolDescendantsVerbose(
            self.0
                .into_iter()
                .map(|(k, v)| {
                    Ok::<_, E>((
                        k.parse::<Txid>().map_err(E::InnerKey)?,
                        v.into_model().map_err(E::InnerValue)?,
                    ))
                })
                .collect::<Result<std::collections::BTreeMap<_, _>, _>>()?,
        ))
    }
}

/// Error when converting a `GetMempoolDescendantsVerbose` type into the model type.
#[derive(Debug)]
pub enum GetMempoolDescendantsVerboseError {
    /// Conversion of the `InnerKey` field failed.
    InnerKey(hex::HexToArrayError),
    /// Conversion of the `InnerValue` field failed.
    InnerValue(MempoolEntryError),
}

impl fmt::Display for GetMempoolDescendantsVerboseError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::InnerKey(ref e) => write_err!(f, "conversion of the `InnerKey` field failed"; e),
            Self::InnerValue(ref e) =>
                write_err!(f, "conversion of the `InnerValue` field failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for GetMempoolDescendantsVerboseError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::InnerKey(ref e) => Some(e),
            Self::InnerValue(ref e) => Some(e),
        }
    }
}

impl GetMempoolDescendantsVerbose1Entry {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::MempoolEntry, MempoolEntryError> {
        use MempoolEntryError as E;

        Ok(model::MempoolEntry {
            vsize: Some(crate::to_u32(self.vsize, "vsize")?),
            size: None, // no raw field; canonical is optional
            weight: Some(crate::to_u32(self.weight, "weight")?),
            time: crate::to_u32(self.time, "time")?,
            height: crate::to_u32(self.height, "height")?,
            descendant_count: u32::try_from(self.descendant_count).map_err(|_| {
                crate::NumericError::Overflow {
                    value: self.descendant_count as i64,
                    field: "descendant_count".to_owned(),
                }
            })?,
            descendant_size: u32::try_from(self.descendant_size).map_err(|_| {
                crate::NumericError::Overflow {
                    value: self.descendant_size as i64,
                    field: "descendant_size".to_owned(),
                }
            })?,
            ancestor_count: u32::try_from(self.ancestor_count).map_err(|_| {
                crate::NumericError::Overflow {
                    value: self.ancestor_count as i64,
                    field: "ancestor_count".to_owned(),
                }
            })?,
            ancestor_size: u32::try_from(self.ancestor_size).map_err(|_| {
                crate::NumericError::Overflow {
                    value: self.ancestor_size as i64,
                    field: "ancestor_size".to_owned(),
                }
            })?,
            wtxid: self.wtxid.parse::<Wtxid>().map_err(E::Wtxid)?,
            fees: self.fees.into_model().map_err(E::Fees)?,
            depends: self
                .depends
                .into_iter()
                .map(|x| x.parse::<Txid>().map_err(E::Depends))
                .collect::<Result<Vec<_>, _>>()?,
            spent_by: self
                .spent_by
                .into_iter()
                .map(|x| x.parse::<Txid>().map_err(E::SpentBy))
                .collect::<Result<Vec<_>, _>>()?,
            bip125_replaceable: Some(self.bip125_replaceable),
            unbroadcast: Some(self.unbroadcast),
        })
    }
}

impl GetMempoolDescendantsVerbose1EntryFees {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::MempoolEntryFees, MempoolEntryFeesError> {
        use MempoolEntryFeesError as E;

        Ok(model::MempoolEntryFees {
            base: Amount::from_btc(self.base).map_err(E::Base)?,
            modified: Amount::from_btc(self.modified).map_err(E::Modified)?,
            ancestor: Amount::from_btc(self.ancestor).map_err(E::Ancestor)?,
            descendant: Amount::from_btc(self.descendant).map_err(E::Descendant)?,
        })
    }
}

impl GetMempoolEntry {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::GetMempoolEntry, GetMempoolEntryError> {
        use GetMempoolEntryError as E;

        Ok(model::GetMempoolEntry(model::MempoolEntry {
            vsize: Some(crate::to_u32(self.vsize, "vsize")?),
            size: None, // no raw field; canonical is optional
            weight: Some(crate::to_u32(self.weight, "weight")?),
            time: crate::to_u32(self.time, "time")?,
            height: crate::to_u32(self.height, "height")?,
            descendant_count: u32::try_from(self.descendant_count).map_err(|_| {
                crate::NumericError::Overflow {
                    value: self.descendant_count as i64,
                    field: "descendant_count".to_owned(),
                }
            })?,
            descendant_size: u32::try_from(self.descendant_size).map_err(|_| {
                crate::NumericError::Overflow {
                    value: self.descendant_size as i64,
                    field: "descendant_size".to_owned(),
                }
            })?,
            ancestor_count: u32::try_from(self.ancestor_count).map_err(|_| {
                crate::NumericError::Overflow {
                    value: self.ancestor_count as i64,
                    field: "ancestor_count".to_owned(),
                }
            })?,
            ancestor_size: u32::try_from(self.ancestor_size).map_err(|_| {
                crate::NumericError::Overflow {
                    value: self.ancestor_size as i64,
                    field: "ancestor_size".to_owned(),
                }
            })?,
            wtxid: self.wtxid.parse::<Wtxid>().map_err(E::Wtxid)?,
            fees: self.fees.into_model().map_err(E::Fees)?,
            depends: self
                .depends
                .into_iter()
                .map(|x| x.parse::<Txid>().map_err(E::Depends))
                .collect::<Result<Vec<_>, _>>()?,
            spent_by: self
                .spent_by
                .into_iter()
                .map(|x| x.parse::<Txid>().map_err(E::SpentBy))
                .collect::<Result<Vec<_>, _>>()?,
            bip125_replaceable: Some(self.bip125_replaceable),
            unbroadcast: Some(self.unbroadcast),
        }))
    }
}

/// Error when converting a `GetMempoolEntry` type into the model type.
#[derive(Debug)]
pub enum GetMempoolEntryError {
    /// Conversion of the `Wtxid` field failed.
    Wtxid(hex::HexToArrayError),
    /// Conversion of the `Fees` field failed.
    Fees(MempoolEntryFeesError),
    /// Conversion of the `Depends` field failed.
    Depends(hex::HexToArrayError),
    /// Conversion of the `SpentBy` field failed.
    SpentBy(hex::HexToArrayError),
    /// Conversion of a numeric type to the expected type failed.
    Numeric(crate::NumericError),
}

impl fmt::Display for GetMempoolEntryError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Wtxid(ref e) => write_err!(f, "conversion of the `Wtxid` field failed"; e),
            Self::Fees(ref e) => write_err!(f, "conversion of the `Fees` field failed"; e),
            Self::Depends(ref e) => write_err!(f, "conversion of the `Depends` field failed"; e),
            Self::SpentBy(ref e) => write_err!(f, "conversion of the `SpentBy` field failed"; e),
            Self::Numeric(ref e) => write_err!(f, "numeric conversion failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for GetMempoolEntryError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Wtxid(ref e) => Some(e),
            Self::Fees(ref e) => Some(e),
            Self::Depends(ref e) => Some(e),
            Self::SpentBy(ref e) => Some(e),
            Self::Numeric(ref e) => Some(e),
        }
    }
}

impl From<crate::NumericError> for GetMempoolEntryError {
    fn from(e: crate::NumericError) -> Self { Self::Numeric(e) }
}

impl GetMempoolEntryFees {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::MempoolEntryFees, MempoolEntryFeesError> {
        use MempoolEntryFeesError as E;

        Ok(model::MempoolEntryFees {
            base: Amount::from_btc(self.base).map_err(E::Base)?,
            modified: Amount::from_btc(self.modified).map_err(E::Modified)?,
            ancestor: Amount::from_btc(self.ancestor).map_err(E::Ancestor)?,
            descendant: Amount::from_btc(self.descendant).map_err(E::Descendant)?,
        })
    }
}

/// Error when converting a `MempoolEntryFees` type into the model type.
#[derive(Debug)]
pub enum MempoolEntryFeesError {
    /// Conversion of the `Base` field failed.
    Base(amount::ParseAmountError),
    /// Conversion of the `Modified` field failed.
    Modified(amount::ParseAmountError),
    /// Conversion of the `Ancestor` field failed.
    Ancestor(amount::ParseAmountError),
    /// Conversion of the `Descendant` field failed.
    Descendant(amount::ParseAmountError),
}

impl fmt::Display for MempoolEntryFeesError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Base(ref e) => write_err!(f, "conversion of the `Base` field failed"; e),
            Self::Modified(ref e) => write_err!(f, "conversion of the `Modified` field failed"; e),
            Self::Ancestor(ref e) => write_err!(f, "conversion of the `Ancestor` field failed"; e),
            Self::Descendant(ref e) =>
                write_err!(f, "conversion of the `Descendant` field failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for MempoolEntryFeesError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Base(ref e) => Some(e),
            Self::Modified(ref e) => Some(e),
            Self::Ancestor(ref e) => Some(e),
            Self::Descendant(ref e) => Some(e),
        }
    }
}

impl GetMempoolInfo {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::GetMempoolInfo, GetMempoolInfoError> {
        use GetMempoolInfoError as E;

        Ok(model::GetMempoolInfo {
            loaded: Some(self.loaded),
            size: u32::try_from(self.size).map_err(|_| crate::NumericError::Overflow {
                value: self.size as i64,
                field: "size".to_owned(),
            })?,
            bytes: u32::try_from(self.bytes).map_err(|_| crate::NumericError::Overflow {
                value: self.bytes as i64,
                field: "bytes".to_owned(),
            })?,
            usage: u32::try_from(self.usage).map_err(|_| crate::NumericError::Overflow {
                value: self.usage as i64,
                field: "usage".to_owned(),
            })?,
            total_fee: Some(self.total_fee),
            max_mempool: crate::to_u32(self.max_mempool, "max_mempool")?,
            mempool_min_fee: crate::btc_per_kb(self.mempool_min_fee).map_err(E::MempoolMinFee)?,
            min_relay_tx_fee: crate::btc_per_kb(self.min_relay_tx_fee).map_err(E::MinRelayTxFee)?,
            incremental_relay_fee: crate::btc_per_kb(self.incremental_relay_fee)
                .map_err(E::IncrementalRelayFee)?,
            unbroadcast_count: Some(u32::try_from(self.unbroadcast_count).map_err(|_| {
                crate::NumericError::Overflow {
                    value: self.unbroadcast_count as i64,
                    field: "unbroadcast_count".to_owned(),
                }
            })?),
            full_rbf: Some(self.full_rbf),
            permit_bare_multisig: Some(self.permit_bare_multisig),
            max_data_carrier_size: Some(crate::to_u64(
                self.max_data_carrier_size,
                "max_data_carrier_size",
            )?),
        })
    }
}

/// Error when converting a `GetMempoolInfo` type into the model type.
#[derive(Debug)]
pub enum GetMempoolInfoError {
    /// Conversion of the `MempoolMinFee` field failed.
    MempoolMinFee(amount::ParseAmountError),
    /// Conversion of the `MinRelayTxFee` field failed.
    MinRelayTxFee(amount::ParseAmountError),
    /// Conversion of the `IncrementalRelayFee` field failed.
    IncrementalRelayFee(amount::ParseAmountError),
    /// Conversion of a numeric type to the expected type failed.
    Numeric(crate::NumericError),
}

impl fmt::Display for GetMempoolInfoError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::MempoolMinFee(ref e) =>
                write_err!(f, "conversion of the `MempoolMinFee` field failed"; e),
            Self::MinRelayTxFee(ref e) =>
                write_err!(f, "conversion of the `MinRelayTxFee` field failed"; e),
            Self::IncrementalRelayFee(ref e) =>
                write_err!(f, "conversion of the `IncrementalRelayFee` field failed"; e),
            Self::Numeric(ref e) => write_err!(f, "numeric conversion failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for GetMempoolInfoError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::MempoolMinFee(ref e) => Some(e),
            Self::MinRelayTxFee(ref e) => Some(e),
            Self::IncrementalRelayFee(ref e) => Some(e),
            Self::Numeric(ref e) => Some(e),
        }
    }
}

impl From<crate::NumericError> for GetMempoolInfoError {
    fn from(e: crate::NumericError) -> Self { Self::Numeric(e) }
}

impl GetTxOutSetInfo {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::GetTxOutSetInfo, GetTxOutSetInfoError> {
        use GetTxOutSetInfoError as E;

        Ok(model::GetTxOutSetInfo {
            height: crate::to_u32(self.height, "height")?,
            best_block: self.best_block.parse::<BlockHash>().map_err(E::BestBlock)?,
            transactions: self
                .transactions
                .map(|x| {
                    u32::try_from(x).map_err(|_| crate::NumericError::Overflow {
                        value: x as i64,
                        field: "transactions".to_owned(),
                    })
                })
                .transpose()?,
            tx_outs: u32::try_from(self.tx_outs).map_err(|_| crate::NumericError::Overflow {
                value: self.tx_outs as i64,
                field: "tx_outs".to_owned(),
            })?,
            bogo_size: u32::try_from(self.bogo_size).map_err(|_| {
                crate::NumericError::Overflow {
                    value: self.bogo_size as i64,
                    field: "bogo_size".to_owned(),
                }
            })?,
            hash_serialized_2: None, // no raw field; canonical is optional
            hash_serialized_3: self.hash_serialized_3,
            disk_size: self
                .disk_size
                .map(|x| {
                    u32::try_from(x).map_err(|_| crate::NumericError::Overflow {
                        value: x as i64,
                        field: "disk_size".to_owned(),
                    })
                })
                .transpose()?,
            total_amount: Amount::from_btc(self.total_amount).map_err(E::TotalAmount)?,
            muhash: self.mu_hash,
            total_unspendable_amount: self
                .total_unspendable_amount
                .map(Amount::from_btc)
                .transpose()
                .map_err(E::TotalUnspendableAmount)?,
            block_info: self
                .block_info
                .map(|x| x.into_model())
                .transpose()
                .map_err(E::BlockInfo)?,
        })
    }
}

/// Error when converting a `GetTxOutSetInfo` type into the model type.
#[derive(Debug)]
pub enum GetTxOutSetInfoError {
    /// Conversion of the `BestBlock` field failed.
    BestBlock(hex::HexToArrayError),
    /// Conversion of the `TotalAmount` field failed.
    TotalAmount(amount::ParseAmountError),
    /// Conversion of the `TotalUnspendableAmount` field failed.
    TotalUnspendableAmount(amount::ParseAmountError),
    /// Conversion of the `BlockInfo` field failed.
    BlockInfo(GetTxOutSetInfoBlockInfoError),
    /// Conversion of a numeric type to the expected type failed.
    Numeric(crate::NumericError),
}

impl fmt::Display for GetTxOutSetInfoError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::BestBlock(ref e) =>
                write_err!(f, "conversion of the `BestBlock` field failed"; e),
            Self::TotalAmount(ref e) =>
                write_err!(f, "conversion of the `TotalAmount` field failed"; e),
            Self::TotalUnspendableAmount(ref e) =>
                write_err!(f, "conversion of the `TotalUnspendableAmount` field failed"; e),
            Self::BlockInfo(ref e) =>
                write_err!(f, "conversion of the `BlockInfo` field failed"; e),
            Self::Numeric(ref e) => write_err!(f, "numeric conversion failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for GetTxOutSetInfoError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::BestBlock(ref e) => Some(e),
            Self::TotalAmount(ref e) => Some(e),
            Self::TotalUnspendableAmount(ref e) => Some(e),
            Self::BlockInfo(ref e) => Some(e),
            Self::Numeric(ref e) => Some(e),
        }
    }
}

impl From<crate::NumericError> for GetTxOutSetInfoError {
    fn from(e: crate::NumericError) -> Self { Self::Numeric(e) }
}

impl GetTxOutSetInfoBlockInfo {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(
        self,
    ) -> Result<model::GetTxOutSetInfoBlockInfo, GetTxOutSetInfoBlockInfoError> {
        use GetTxOutSetInfoBlockInfoError as E;

        Ok(model::GetTxOutSetInfoBlockInfo {
            prevout_spent: Amount::from_btc(self.prevout_spent).map_err(E::PrevoutSpent)?,
            coinbase: Amount::from_btc(self.coinbase).map_err(E::Coinbase)?,
            new_outputs_ex_coinbase: Amount::from_btc(self.new_outputs_ex_coinbase)
                .map_err(E::NewOutputsExCoinbase)?,
            unspendable: Amount::from_btc(self.unspendable).map_err(E::Unspendable)?,
            unspendables: self.unspendables.into_model().map_err(E::Unspendables)?,
        })
    }
}

/// Error when converting a `GetTxOutSetInfoBlockInfo` type into the model type.
#[derive(Debug)]
pub enum GetTxOutSetInfoBlockInfoError {
    /// Conversion of the `PrevoutSpent` field failed.
    PrevoutSpent(amount::ParseAmountError),
    /// Conversion of the `Coinbase` field failed.
    Coinbase(amount::ParseAmountError),
    /// Conversion of the `NewOutputsExCoinbase` field failed.
    NewOutputsExCoinbase(amount::ParseAmountError),
    /// Conversion of the `Unspendable` field failed.
    Unspendable(amount::ParseAmountError),
    /// Conversion of the `Unspendables` field failed.
    Unspendables(GetTxOutSetInfoUnspendablesError),
}

impl fmt::Display for GetTxOutSetInfoBlockInfoError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::PrevoutSpent(ref e) =>
                write_err!(f, "conversion of the `PrevoutSpent` field failed"; e),
            Self::Coinbase(ref e) => write_err!(f, "conversion of the `Coinbase` field failed"; e),
            Self::NewOutputsExCoinbase(ref e) =>
                write_err!(f, "conversion of the `NewOutputsExCoinbase` field failed"; e),
            Self::Unspendable(ref e) =>
                write_err!(f, "conversion of the `Unspendable` field failed"; e),
            Self::Unspendables(ref e) =>
                write_err!(f, "conversion of the `Unspendables` field failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for GetTxOutSetInfoBlockInfoError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::PrevoutSpent(ref e) => Some(e),
            Self::Coinbase(ref e) => Some(e),
            Self::NewOutputsExCoinbase(ref e) => Some(e),
            Self::Unspendable(ref e) => Some(e),
            Self::Unspendables(ref e) => Some(e),
        }
    }
}

impl GetTxOutSetInfoBlockInfoUnspendables {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(
        self,
    ) -> Result<model::GetTxOutSetInfoUnspendables, GetTxOutSetInfoUnspendablesError> {
        use GetTxOutSetInfoUnspendablesError as E;

        Ok(model::GetTxOutSetInfoUnspendables {
            genesis_block: Amount::from_btc(self.genesis_block).map_err(E::GenesisBlock)?,
            bip30: Amount::from_btc(self.bip30).map_err(E::Bip30)?,
            scripts: Amount::from_btc(self.scripts).map_err(E::Scripts)?,
            unclaimed_rewards: Amount::from_btc(self.unclaimed_rewards)
                .map_err(E::UnclaimedRewards)?,
        })
    }
}

/// Error when converting a `GetTxOutSetInfoUnspendables` type into the model type.
#[derive(Debug)]
pub enum GetTxOutSetInfoUnspendablesError {
    /// Conversion of the `GenesisBlock` field failed.
    GenesisBlock(amount::ParseAmountError),
    /// Conversion of the `Bip30` field failed.
    Bip30(amount::ParseAmountError),
    /// Conversion of the `Scripts` field failed.
    Scripts(amount::ParseAmountError),
    /// Conversion of the `UnclaimedRewards` field failed.
    UnclaimedRewards(amount::ParseAmountError),
}

impl fmt::Display for GetTxOutSetInfoUnspendablesError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::GenesisBlock(ref e) =>
                write_err!(f, "conversion of the `GenesisBlock` field failed"; e),
            Self::Bip30(ref e) => write_err!(f, "conversion of the `Bip30` field failed"; e),
            Self::Scripts(ref e) => write_err!(f, "conversion of the `Scripts` field failed"; e),
            Self::UnclaimedRewards(ref e) =>
                write_err!(f, "conversion of the `UnclaimedRewards` field failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for GetTxOutSetInfoUnspendablesError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::GenesisBlock(ref e) => Some(e),
            Self::Bip30(ref e) => Some(e),
            Self::Scripts(ref e) => Some(e),
            Self::UnclaimedRewards(ref e) => Some(e),
        }
    }
}

impl LoadTxOutSet {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::LoadTxOutSet, LoadTxOutSetError> {
        use LoadTxOutSetError as E;

        Ok(model::LoadTxOutSet {
            // codegen-correct conversion (u64): `self.coins_loaded`.
            // TODO(compat): canonical types `coins_loaded` as `Amount` but Core returns a UTXO count (corepc_bugs_backlog.md #1). Routed through a wrong placeholder in `compatibility` to compile.
            coins_loaded: compatibility::load_tx_out_set_coins_loaded(self.coins_loaded),
            tip_hash: self.tip_hash.parse::<BlockHash>().map_err(E::TipHash)?,
            base_height: crate::to_u32(self.base_height, "base_height")?,
            path: self.path,
        })
    }
}

/// Error when converting a `LoadTxOutSet` type into the model type.
#[derive(Debug)]
pub enum LoadTxOutSetError {
    /// Conversion of the `TipHash` field failed.
    TipHash(hex::HexToArrayError),
    /// Conversion of a numeric type to the expected type failed.
    Numeric(crate::NumericError),
}

impl fmt::Display for LoadTxOutSetError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::TipHash(ref e) => write_err!(f, "conversion of the `TipHash` field failed"; e),
            Self::Numeric(ref e) => write_err!(f, "numeric conversion failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for LoadTxOutSetError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::TipHash(ref e) => Some(e),
            Self::Numeric(ref e) => Some(e),
        }
    }
}

impl From<crate::NumericError> for LoadTxOutSetError {
    fn from(e: crate::NumericError) -> Self { Self::Numeric(e) }
}

impl ScanTxOutSetVariant0 {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::ScanTxOutSetStart, ScanTxOutSetStartError> {
        use ScanTxOutSetStartError as E;

        Ok(model::ScanTxOutSetStart {
            success: self.success,
            tx_outs: Some(crate::to_u64(self.tx_outs, "tx_outs")?),
            height: Some(crate::to_u64(self.height, "height")?),
            best_block: Some(self.best_block.parse::<BlockHash>().map_err(E::BestBlock)?),
            unspents: self
                .unspents
                .into_iter()
                .map(|x| x.into_model().map_err(E::Unspents))
                .collect::<Result<Vec<_>, _>>()?,
            total_amount: Amount::from_btc(self.total_amount).map_err(E::TotalAmount)?,
        })
    }
}

/// Error when converting a `ScanTxOutSetStart` type into the model type.
#[derive(Debug)]
pub enum ScanTxOutSetStartError {
    /// Conversion of the `BestBlock` field failed.
    BestBlock(hex::HexToArrayError),
    /// Conversion of the `Unspents` field failed.
    Unspents(ScanTxOutSetUnspentError),
    /// Conversion of the `TotalAmount` field failed.
    TotalAmount(amount::ParseAmountError),
    /// Conversion of a numeric type to the expected type failed.
    Numeric(crate::NumericError),
}

impl fmt::Display for ScanTxOutSetStartError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::BestBlock(ref e) =>
                write_err!(f, "conversion of the `BestBlock` field failed"; e),
            Self::Unspents(ref e) => write_err!(f, "conversion of the `Unspents` field failed"; e),
            Self::TotalAmount(ref e) =>
                write_err!(f, "conversion of the `TotalAmount` field failed"; e),
            Self::Numeric(ref e) => write_err!(f, "numeric conversion failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for ScanTxOutSetStartError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::BestBlock(ref e) => Some(e),
            Self::Unspents(ref e) => Some(e),
            Self::TotalAmount(ref e) => Some(e),
            Self::Numeric(ref e) => Some(e),
        }
    }
}

impl From<crate::NumericError> for ScanTxOutSetStartError {
    fn from(e: crate::NumericError) -> Self { Self::Numeric(e) }
}

impl ScanTxOutSetVariant0UnspentsItem {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::ScanTxOutSetUnspent, ScanTxOutSetUnspentError> {
        use ScanTxOutSetUnspentError as E;

        Ok(model::ScanTxOutSetUnspent {
            txid: self.txid.parse::<Txid>().map_err(E::Txid)?,
            vout: crate::to_u32(self.vout, "vout")?,
            script_pubkey: ScriptBuf::from_hex(&self.script_pub_key).map_err(E::ScriptPubkey)?,
            descriptor: None, // no raw field; canonical is optional
            amount: Amount::from_btc(self.amount).map_err(E::Amount)?,
            coinbase: Some(self.coinbase),
            height: crate::to_u64(self.height, "height")?,
            block_hash: Some(self.block_hash.parse::<BlockHash>().map_err(E::BlockHash)?),
            confirmations: Some(crate::to_u64(self.confirmations, "confirmations")?),
        })
    }
}

/// Error when converting a `ScanTxOutSetUnspent` type into the model type.
#[derive(Debug)]
pub enum ScanTxOutSetUnspentError {
    /// Conversion of the `Txid` field failed.
    Txid(hex::HexToArrayError),
    /// Conversion of the `ScriptPubkey` field failed.
    ScriptPubkey(hex::HexToBytesError),
    /// Conversion of the `Amount` field failed.
    Amount(amount::ParseAmountError),
    /// Conversion of the `BlockHash` field failed.
    BlockHash(hex::HexToArrayError),
    /// Conversion of a numeric type to the expected type failed.
    Numeric(crate::NumericError),
}

impl fmt::Display for ScanTxOutSetUnspentError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Txid(ref e) => write_err!(f, "conversion of the `Txid` field failed"; e),
            Self::ScriptPubkey(ref e) =>
                write_err!(f, "conversion of the `ScriptPubkey` field failed"; e),
            Self::Amount(ref e) => write_err!(f, "conversion of the `Amount` field failed"; e),
            Self::BlockHash(ref e) =>
                write_err!(f, "conversion of the `BlockHash` field failed"; e),
            Self::Numeric(ref e) => write_err!(f, "numeric conversion failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for ScanTxOutSetUnspentError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Txid(ref e) => Some(e),
            Self::ScriptPubkey(ref e) => Some(e),
            Self::Amount(ref e) => Some(e),
            Self::BlockHash(ref e) => Some(e),
            Self::Numeric(ref e) => Some(e),
        }
    }
}

impl From<crate::NumericError> for ScanTxOutSetUnspentError {
    fn from(e: crate::NumericError) -> Self { Self::Numeric(e) }
}

impl VerifyTxOutProof {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::VerifyTxOutProof, VerifyTxOutProofError> {
        use VerifyTxOutProofError as E;

        Ok(model::VerifyTxOutProof(
            self.0
                .into_iter()
                .map(|x| x.parse::<Txid>().map_err(E::Inner))
                .collect::<Result<Vec<_>, _>>()?,
        ))
    }
}

/// Error when converting a `VerifyTxOutProof` type into the model type.
#[derive(Debug)]
pub enum VerifyTxOutProofError {
    /// Conversion of the `Inner` field failed.
    Inner(hex::HexToArrayError),
}

impl fmt::Display for VerifyTxOutProofError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Inner(ref e) => write_err!(f, "conversion of the `Inner` field failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for VerifyTxOutProofError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Inner(ref e) => Some(e),
        }
    }
}

impl WaitForBlock {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::WaitForBlock, WaitForBlockError> {
        use WaitForBlockError as E;

        Ok(model::WaitForBlock {
            hash: self.hash.parse::<BlockHash>().map_err(E::Hash)?,
            height: crate::to_u32(self.height, "height")?,
        })
    }
}

/// Error when converting a `WaitForBlock` type into the model type.
#[derive(Debug)]
pub enum WaitForBlockError {
    /// Conversion of the `Hash` field failed.
    Hash(hex::HexToArrayError),
    /// Conversion of a numeric type to the expected type failed.
    Numeric(crate::NumericError),
}

impl fmt::Display for WaitForBlockError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Hash(ref e) => write_err!(f, "conversion of the `Hash` field failed"; e),
            Self::Numeric(ref e) => write_err!(f, "numeric conversion failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for WaitForBlockError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Hash(ref e) => Some(e),
            Self::Numeric(ref e) => Some(e),
        }
    }
}

impl From<crate::NumericError> for WaitForBlockError {
    fn from(e: crate::NumericError) -> Self { Self::Numeric(e) }
}

impl WaitForBlockHeight {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::WaitForBlockHeight, WaitForBlockHeightError> {
        use WaitForBlockHeightError as E;

        Ok(model::WaitForBlockHeight {
            hash: self.hash.parse::<BlockHash>().map_err(E::Hash)?,
            height: crate::to_u32(self.height, "height")?,
        })
    }
}

/// Error when converting a `WaitForBlockHeight` type into the model type.
#[derive(Debug)]
pub enum WaitForBlockHeightError {
    /// Conversion of the `Hash` field failed.
    Hash(hex::HexToArrayError),
    /// Conversion of a numeric type to the expected type failed.
    Numeric(crate::NumericError),
}

impl fmt::Display for WaitForBlockHeightError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Hash(ref e) => write_err!(f, "conversion of the `Hash` field failed"; e),
            Self::Numeric(ref e) => write_err!(f, "numeric conversion failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for WaitForBlockHeightError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Hash(ref e) => Some(e),
            Self::Numeric(ref e) => Some(e),
        }
    }
}

impl From<crate::NumericError> for WaitForBlockHeightError {
    fn from(e: crate::NumericError) -> Self { Self::Numeric(e) }
}

impl WaitForNewBlock {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::WaitForNewBlock, WaitForNewBlockError> {
        use WaitForNewBlockError as E;

        Ok(model::WaitForNewBlock {
            hash: self.hash.parse::<BlockHash>().map_err(E::Hash)?,
            height: crate::to_u32(self.height, "height")?,
        })
    }
}

/// Error when converting a `WaitForNewBlock` type into the model type.
#[derive(Debug)]
pub enum WaitForNewBlockError {
    /// Conversion of the `Hash` field failed.
    Hash(hex::HexToArrayError),
    /// Conversion of a numeric type to the expected type failed.
    Numeric(crate::NumericError),
}

impl fmt::Display for WaitForNewBlockError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Hash(ref e) => write_err!(f, "conversion of the `Hash` field failed"; e),
            Self::Numeric(ref e) => write_err!(f, "numeric conversion failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for WaitForNewBlockError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Hash(ref e) => Some(e),
            Self::Numeric(ref e) => Some(e),
        }
    }
}

impl From<crate::NumericError> for WaitForNewBlockError {
    fn from(e: crate::NumericError) -> Self { Self::Numeric(e) }
}
