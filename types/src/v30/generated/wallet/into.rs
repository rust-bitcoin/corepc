// SPDX-License-Identifier: CC0-1.0

//! Auto-generated `into_model` conversions for Bitcoin Core `30` - wallet.
//!
//! Generated by `codegen`. Do not edit by hand, re-run `just codegen` to regenerate. Each
//! conversion turns a raw response type (from the sibling `mod.rs`) into its shared,
//! version-nonspecific `crate::model` counterpart. Conversions the canonical types get
//! wrong are routed through `crate::v30::generated::compatibility`; see that file and
//! `corepc_bugs_backlog.md`.

// `unreachable_code` is allowed for the half-done `todo!()` reconstructions (see the
// per-site TODOs); they diverge, making later struct fields unreachable.
#![allow(unreachable_code, unused_imports)]

use core::fmt;

use bitcoin::address::{self, NetworkUnchecked};
use bitcoin::consensus::encode;
use bitcoin::error::UnprefixedHexError;
use bitcoin::hashes::{hash160, sha256};
use bitcoin::{
    amount, block, hex, network, psbt, Address, Amount, Block, BlockHash, CompactTarget, FeeRate,
    Network, Psbt, ScriptBuf, Sequence, SignedAmount, Target, Transaction, TxMerkleNode, Txid,
    Weight, Work, Wtxid,
};

use super::*;
use crate::error::write_err;
use crate::model;
use crate::v30::generated::compatibility;

impl CreateWallet {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::CreateWallet, CreateWalletError> {
        use CreateWalletError as E;

        Ok(model::CreateWallet { name: self.name, warnings: self.warnings.unwrap_or_default() })
    }
}

/// Error when converting a `CreateWallet` type into the model type.
#[derive(Debug)]
pub enum CreateWalletError {}

impl fmt::Display for CreateWalletError {
    fn fmt(&self, _f: &mut fmt::Formatter) -> fmt::Result { match *self {} }
}

#[cfg(feature = "std")]
impl std::error::Error for CreateWalletError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> { match *self {} }
}

impl GetBalance {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::GetBalance, GetBalanceError> {
        use GetBalanceError as E;

        Ok(model::GetBalance(Amount::from_btc(self.0).map_err(E::Inner)?))
    }
}

/// Error when converting a `GetBalance` type into the model type.
#[derive(Debug)]
pub enum GetBalanceError {
    /// Conversion of the `Inner` field failed.
    Inner(amount::ParseAmountError),
}

impl fmt::Display for GetBalanceError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Inner(ref e) => write_err!(f, "conversion of the `Inner` field failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for GetBalanceError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Inner(ref e) => Some(e),
        }
    }
}

impl GetBalancesMine {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::GetBalancesMine, GetBalancesMineError> {
        use GetBalancesMineError as E;

        Ok(model::GetBalancesMine {
            trusted: Amount::from_btc(self.trusted).map_err(E::Trusted)?,
            untrusted_pending: Amount::from_btc(self.untrusted_pending)
                .map_err(E::UntrustedPending)?,
            immature: Amount::from_btc(self.immature).map_err(E::Immature)?,
            used: self.used.map(Amount::from_btc).transpose().map_err(E::Used)?,
        })
    }
}

/// Error when converting a `GetBalancesMine` type into the model type.
#[derive(Debug)]
pub enum GetBalancesMineError {
    /// Conversion of the `Trusted` field failed.
    Trusted(amount::ParseAmountError),
    /// Conversion of the `UntrustedPending` field failed.
    UntrustedPending(amount::ParseAmountError),
    /// Conversion of the `Immature` field failed.
    Immature(amount::ParseAmountError),
    /// Conversion of the `Used` field failed.
    Used(amount::ParseAmountError),
}

impl fmt::Display for GetBalancesMineError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Trusted(ref e) => write_err!(f, "conversion of the `Trusted` field failed"; e),
            Self::UntrustedPending(ref e) =>
                write_err!(f, "conversion of the `UntrustedPending` field failed"; e),
            Self::Immature(ref e) => write_err!(f, "conversion of the `Immature` field failed"; e),
            Self::Used(ref e) => write_err!(f, "conversion of the `Used` field failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for GetBalancesMineError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Trusted(ref e) => Some(e),
            Self::UntrustedPending(ref e) => Some(e),
            Self::Immature(ref e) => Some(e),
            Self::Used(ref e) => Some(e),
        }
    }
}

impl GetNewAddress {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::GetNewAddress, GetNewAddressError> {
        use GetNewAddressError as E;

        Ok(model::GetNewAddress(self.0.parse::<Address<NetworkUnchecked>>().map_err(E::Inner)?))
    }
}

/// Error when converting a `GetNewAddress` type into the model type.
#[derive(Debug)]
pub enum GetNewAddressError {
    /// Conversion of the `Inner` field failed.
    Inner(address::ParseError),
}

impl fmt::Display for GetNewAddressError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Inner(ref e) => write_err!(f, "conversion of the `Inner` field failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for GetNewAddressError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Inner(ref e) => Some(e),
        }
    }
}

impl GetRawChangeAddress {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::GetRawChangeAddress, GetRawChangeAddressError> {
        use GetRawChangeAddressError as E;

        Ok(model::GetRawChangeAddress(
            self.0.parse::<Address<NetworkUnchecked>>().map_err(E::Inner)?,
        ))
    }
}

/// Error when converting a `GetRawChangeAddress` type into the model type.
#[derive(Debug)]
pub enum GetRawChangeAddressError {
    /// Conversion of the `Inner` field failed.
    Inner(address::ParseError),
}

impl fmt::Display for GetRawChangeAddressError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Inner(ref e) => write_err!(f, "conversion of the `Inner` field failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for GetRawChangeAddressError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Inner(ref e) => Some(e),
        }
    }
}

impl GetReceivedByAddress {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::GetReceivedByAddress, GetReceivedByAddressError> {
        use GetReceivedByAddressError as E;

        Ok(model::GetReceivedByAddress(Amount::from_btc(self.0).map_err(E::Inner)?))
    }
}

/// Error when converting a `GetReceivedByAddress` type into the model type.
#[derive(Debug)]
pub enum GetReceivedByAddressError {
    /// Conversion of the `Inner` field failed.
    Inner(amount::ParseAmountError),
}

impl fmt::Display for GetReceivedByAddressError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Inner(ref e) => write_err!(f, "conversion of the `Inner` field failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for GetReceivedByAddressError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Inner(ref e) => Some(e),
        }
    }
}

impl GetReceivedByLabel {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::GetReceivedByLabel, GetReceivedByLabelError> {
        use GetReceivedByLabelError as E;

        Ok(model::GetReceivedByLabel(Amount::from_btc(self.0).map_err(E::Inner)?))
    }
}

/// Error when converting a `GetReceivedByLabel` type into the model type.
#[derive(Debug)]
pub enum GetReceivedByLabelError {
    /// Conversion of the `Inner` field failed.
    Inner(amount::ParseAmountError),
}

impl fmt::Display for GetReceivedByLabelError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Inner(ref e) => write_err!(f, "conversion of the `Inner` field failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for GetReceivedByLabelError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Inner(ref e) => Some(e),
        }
    }
}

impl ListLockUnspentItem {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::ListLockUnspentItem, ListLockUnspentItemError> {
        use ListLockUnspentItemError as E;

        Ok(model::ListLockUnspentItem {
            txid: self.txid.parse::<Txid>().map_err(E::Txid)?,
            vout: crate::to_u32(self.vout, "vout")?,
        })
    }
}

/// Error when converting a `ListLockUnspentItem` type into the model type.
#[derive(Debug)]
pub enum ListLockUnspentItemError {
    /// Conversion of the `Txid` field failed.
    Txid(hex::HexToArrayError),
    /// Conversion of a numeric type to the expected type failed.
    Numeric(crate::NumericError),
}

impl fmt::Display for ListLockUnspentItemError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Txid(ref e) => write_err!(f, "conversion of the `Txid` field failed"; e),
            Self::Numeric(ref e) => write_err!(f, "numeric conversion failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for ListLockUnspentItemError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Txid(ref e) => Some(e),
            Self::Numeric(ref e) => Some(e),
        }
    }
}

impl From<crate::NumericError> for ListLockUnspentItemError {
    fn from(e: crate::NumericError) -> Self { Self::Numeric(e) }
}

impl ListReceivedByAddressItem {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(
        self,
    ) -> Result<model::ListReceivedByAddressItem, ListReceivedByAddressItemError> {
        use ListReceivedByAddressItemError as E;

        Ok(model::ListReceivedByAddressItem {
            involves_watch_only: None, // no raw field; canonical is optional
            address: self.address.parse::<Address<NetworkUnchecked>>().map_err(E::Address)?,
            amount: Amount::from_btc(self.amount).map_err(E::Amount)?,
            confirmations: self.confirmations,
            label: self.label,
            txids: self
                .txids
                .into_iter()
                .map(|x| x.parse::<Txid>().map_err(E::Txids))
                .collect::<Result<Vec<_>, _>>()?,
        })
    }
}

/// Error when converting a `ListReceivedByAddressItem` type into the model type.
#[derive(Debug)]
pub enum ListReceivedByAddressItemError {
    /// Conversion of the `Address` field failed.
    Address(address::ParseError),
    /// Conversion of the `Amount` field failed.
    Amount(amount::ParseAmountError),
    /// Conversion of the `Txids` field failed.
    Txids(hex::HexToArrayError),
}

impl fmt::Display for ListReceivedByAddressItemError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Address(ref e) => write_err!(f, "conversion of the `Address` field failed"; e),
            Self::Amount(ref e) => write_err!(f, "conversion of the `Amount` field failed"; e),
            Self::Txids(ref e) => write_err!(f, "conversion of the `Txids` field failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for ListReceivedByAddressItemError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Address(ref e) => Some(e),
            Self::Amount(ref e) => Some(e),
            Self::Txids(ref e) => Some(e),
        }
    }
}

impl ListReceivedByLabelItem {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(
        self,
    ) -> Result<model::ListReceivedByLabelItem, ListReceivedByLabelItemError> {
        use ListReceivedByLabelItemError as E;

        Ok(model::ListReceivedByLabelItem {
            involves_watch_only: None, // no raw field; canonical is optional
            amount: Amount::from_btc(self.amount).map_err(E::Amount)?,
            confirmations: crate::to_u32(self.confirmations, "confirmations")?,
            label: self.label,
        })
    }
}

/// Error when converting a `ListReceivedByLabelItem` type into the model type.
#[derive(Debug)]
pub enum ListReceivedByLabelItemError {
    /// Conversion of the `Amount` field failed.
    Amount(amount::ParseAmountError),
    /// Conversion of a numeric type to the expected type failed.
    Numeric(crate::NumericError),
}

impl fmt::Display for ListReceivedByLabelItemError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Amount(ref e) => write_err!(f, "conversion of the `Amount` field failed"; e),
            Self::Numeric(ref e) => write_err!(f, "numeric conversion failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for ListReceivedByLabelItemError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Amount(ref e) => Some(e),
            Self::Numeric(ref e) => Some(e),
        }
    }
}

impl From<crate::NumericError> for ListReceivedByLabelItemError {
    fn from(e: crate::NumericError) -> Self { Self::Numeric(e) }
}

impl LoadWallet {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::LoadWallet, LoadWalletError> {
        use LoadWalletError as E;

        Ok(model::LoadWallet { name: self.name, warnings: self.warnings.unwrap_or_default() })
    }
}

/// Error when converting a `LoadWallet` type into the model type.
#[derive(Debug)]
pub enum LoadWalletError {}

impl fmt::Display for LoadWalletError {
    fn fmt(&self, _f: &mut fmt::Formatter) -> fmt::Result { match *self {} }
}

#[cfg(feature = "std")]
impl std::error::Error for LoadWalletError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> { match *self {} }
}

impl RescanBlockchain {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::RescanBlockchain, RescanBlockchainError> {
        use RescanBlockchainError as E;

        Ok(model::RescanBlockchain {
            start_height: crate::to_u32(self.start_height, "start_height")?,
            stop_height: crate::to_u32(self.stop_height, "stop_height")?,
        })
    }
}

/// Error when converting a `RescanBlockchain` type into the model type.
#[derive(Debug)]
pub enum RescanBlockchainError {
    /// Conversion of a numeric type to the expected type failed.
    Numeric(crate::NumericError),
}

impl fmt::Display for RescanBlockchainError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Numeric(ref e) => write_err!(f, "numeric conversion failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for RescanBlockchainError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Numeric(ref e) => Some(e),
        }
    }
}

impl From<crate::NumericError> for RescanBlockchainError {
    fn from(e: crate::NumericError) -> Self { Self::Numeric(e) }
}

impl SendAll {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::SendAll, SendAllError> {
        use SendAllError as E;

        Ok(model::SendAll {
            complete: self.complete,
            txid: self.txid.map(|x| x.parse::<Txid>()).transpose().map_err(E::Txid)?,
            hex: self
                .hex
                .map(|x| encode::deserialize_hex::<Transaction>(&x))
                .transpose()
                .map_err(E::Hex)?,
            psbt: self.psbt.map(|x| x.parse::<Psbt>()).transpose().map_err(E::Psbt)?,
        })
    }
}

/// Error when converting a `SendAll` type into the model type.
#[derive(Debug)]
pub enum SendAllError {
    /// Conversion of the `Txid` field failed.
    Txid(hex::HexToArrayError),
    /// Conversion of the `Hex` field failed.
    Hex(encode::FromHexError),
    /// Conversion of the `Psbt` field failed.
    Psbt(psbt::PsbtParseError),
}

impl fmt::Display for SendAllError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Txid(ref e) => write_err!(f, "conversion of the `Txid` field failed"; e),
            Self::Hex(ref e) => write_err!(f, "conversion of the `Hex` field failed"; e),
            Self::Psbt(ref e) => write_err!(f, "conversion of the `Psbt` field failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for SendAllError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Txid(ref e) => Some(e),
            Self::Hex(ref e) => Some(e),
            Self::Psbt(ref e) => Some(e),
        }
    }
}

impl SimulateRawTransaction {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::SimulateRawTransaction, SimulateRawTransactionError> {
        use SimulateRawTransactionError as E;

        Ok(model::SimulateRawTransaction {
            balance_change: SignedAmount::from_btc(self.balance_change)
                .map_err(E::BalanceChange)?,
        })
    }
}

/// Error when converting a `SimulateRawTransaction` type into the model type.
#[derive(Debug)]
pub enum SimulateRawTransactionError {
    /// Conversion of the `BalanceChange` field failed.
    BalanceChange(amount::ParseAmountError),
}

impl fmt::Display for SimulateRawTransactionError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::BalanceChange(ref e) =>
                write_err!(f, "conversion of the `BalanceChange` field failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for SimulateRawTransactionError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::BalanceChange(ref e) => Some(e),
        }
    }
}

impl UnloadWallet {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::UnloadWallet, UnloadWalletError> {
        use UnloadWalletError as E;

        Ok(model::UnloadWallet { warnings: self.warnings.unwrap_or_default() })
    }
}

/// Error when converting a `UnloadWallet` type into the model type.
#[derive(Debug)]
pub enum UnloadWalletError {}

impl fmt::Display for UnloadWalletError {
    fn fmt(&self, _f: &mut fmt::Formatter) -> fmt::Result { match *self {} }
}

#[cfg(feature = "std")]
impl std::error::Error for UnloadWalletError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> { match *self {} }
}

impl WalletDisplayAddress {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::WalletDisplayAddress, WalletDisplayAddressError> {
        use WalletDisplayAddressError as E;

        Ok(model::WalletDisplayAddress {
            address: self.address.parse::<Address<NetworkUnchecked>>().map_err(E::Address)?,
        })
    }
}

/// Error when converting a `WalletDisplayAddress` type into the model type.
#[derive(Debug)]
pub enum WalletDisplayAddressError {
    /// Conversion of the `Address` field failed.
    Address(address::ParseError),
}

impl fmt::Display for WalletDisplayAddressError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Address(ref e) => write_err!(f, "conversion of the `Address` field failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for WalletDisplayAddressError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Address(ref e) => Some(e),
        }
    }
}

impl WalletProcessPsbt {
    /// Converts the raw type into the version-nonspecific model type.
    pub fn into_model(self) -> Result<model::WalletProcessPsbt, WalletProcessPsbtError> {
        use WalletProcessPsbtError as E;

        Ok(model::WalletProcessPsbt {
            psbt: self.psbt.parse::<Psbt>().map_err(E::Psbt)?,
            complete: self.complete,
            hex: self
                .hex
                .map(|x| encode::deserialize_hex::<Transaction>(&x))
                .transpose()
                .map_err(E::Hex)?,
        })
    }
}

/// Error when converting a `WalletProcessPsbt` type into the model type.
#[derive(Debug)]
pub enum WalletProcessPsbtError {
    /// Conversion of the `Psbt` field failed.
    Psbt(psbt::PsbtParseError),
    /// Conversion of the `Hex` field failed.
    Hex(encode::FromHexError),
}

impl fmt::Display for WalletProcessPsbtError {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match *self {
            Self::Psbt(ref e) => write_err!(f, "conversion of the `Psbt` field failed"; e),
            Self::Hex(ref e) => write_err!(f, "conversion of the `Hex` field failed"; e),
        }
    }
}

#[cfg(feature = "std")]
impl std::error::Error for WalletProcessPsbtError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match *self {
            Self::Psbt(ref e) => Some(e),
            Self::Hex(ref e) => Some(e),
        }
    }
}
